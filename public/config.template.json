{
    "APP_DEBUG": ${APP_DEBUG},
    "APP_ENV": "${APP_ENV}",
    "APP_URL": "${APP_URL}",
    "BROADCAST_URL": "${BROADCAST_URL}",
    "PUSHER_APP_KEY": "${PUSHER_APP_KEY}",
    "PUSHER_SOCKET_HOST": "${PUSHER_HOST}",
    "PUSHER_PORT": ${PUSHER_PORT},
    "PUSHER_SCHEME": "${PUSHER_SCHEME}"
}